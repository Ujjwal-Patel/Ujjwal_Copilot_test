export enum TaskPriority {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low'
}

export enum TaskStatus {
  IN_PROGRESS = 'In Progress',
  NOT_AVAILABLE = 'Not Available',
  COMPLETED = 'Completed',
  DUE = 'Due'
}
